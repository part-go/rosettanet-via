syntax = "proto3";

package register;
option go_package = "./register";

message Boolean {
    bool result = 1;
}

message RegisterReq {
    string taskId=1;
    string partyId=2;
    string serviceType=3;
    string address=4;
}

service RegisterService {
    rpc Register(RegisterReq) returns (Boolean);
}